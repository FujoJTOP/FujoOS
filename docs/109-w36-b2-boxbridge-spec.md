# 109 · W36 — B-2 v0 规格：BOX-BRIDGE 边界解决设计（zcode 方案）

> 来源: zcode B-2 v0 完整方案（2025-06 评述） · 前置: docs/108（理论: 拆/接/出界）
> docs/105 §6（FUFORALL 立场） · 状态: **规格 v0.1（v0 裁剪见 §8；v0.1 弱点闭合见 §11，未开工）**
> 一句话: **五件架构件 + 六条边界全部闭合，且每件都长在现有零件上
> （域/cap_exec/R3 帧/0x8314/审计环）—— B-2 v0 以本文为规格骨架。**

## 0. 总原则与完备性定义

**判定树**：拆得开的拆（LEGO·开源语义转录）→ 分不开的接（BOX-BRIDGE·外部能力管道）→ 接不得的出界（EXCLUDED·有名有姓）。

**完备性搬家（三级）**：语义完备性（不再要求）→ 接口完备性（动词词汇表有限可枚举）→ 声明完备性（EXCLUDED 枚举 + 能力矩阵可观测）。**FUFORALL 的"完备" = 三者合并，且只在"使用"意义下成立。**

## 1. 五件架构件

| 件 | 职责 | 现有零件复用 | 新增范围 |
|---|---|---|---|
| ① 能力注册表（Provider Registry） | `(动词 V, 平台 P, 供应商 id, 状态, 指标)` —— 盒子 = "在 Windows 上经 winword 提供 PDF 的**供应商**"，不是"转 PDF 能力" | cap_exec 域（注册 = 授权动作门槛，域 = 该供应商端口） | 注册表数据结构 + syscall 查询 |
| ② 动词词汇表（Verb Vocabulary） | 动词集 \(V = \{\) 转 PDF, 查文档, …\(\}\)，几十量级封顶 | — | 枚举表（数据为主） |
| ③ 帧协议（BOX Frames v2） | 命令进 / 结构化结果 + 状态出；复用 v2 帧头 `seq/t0/evw/crit/TTL`，盒子回包受 R3 时延裁决（陈旧/过期照旧丢弃） | `ai.rs` 帧头 + `wait_rsp` 的 crit/ttl DISCARD（M112） | 新 kind 通道（模型通道的胞弟） |
| ④ 产物检疫门（Artifact Gate） | **S1 扩展**：防越权从"动作"扩展到"产物"；盒返回值 = 不可信数据 | 审计环（新增"产物拒收"动作码） | 魔数/类型白名单 → 大小上限 → 格式校验 → 禁执行语义 |
| ⑤ 双列质量台账（Dual Ledger） | 列1 履约率（接口层：返回成功 + schema 校验）/ 列2 下游成功率（语义层 = 消费端验证）；域宽 = f(质量) | 0x8314 族（QUAL_HIT/TOTAL/UP_RUN） | 双列扩展，与模型同尺度 |
| ⑥ Box Adapter（宿主侧，v0.1 增补，见 §11.1） | 唯一认识"盒世界自动化"的组件：动词帧 → 盒世界调用；盒世界结果 → 产物+状态帧 | —（宿主侧，不装载） | Adapter 契约声明 + 版本 |

**动词调度器（Router）**：动词 V → 供应商链，优先级固定 **LEGO > BOX**——原生等价物存在时盒子永不做主路，只补缺。

## 2. 六条边界逐条解决

| 边界 | 方案 | 闭合点 |
|---|---|---|
| 1 · 能力依赖外部世界 | 换成**可替换的供应商**：任何盒子挂掉 = 该供应商缺席，动词仍可由其他供应商/原生途径服务；**只有全部供应商缺席才有缺货语义** | 单一来源锁定被消除 |
| 2 · 缺席语义 | 粒度 **per-verb × per-provider**（不是 per-box）；缺席 = **声明**（矩阵显示"零供应商"）+ 审计 + S3 可读，**不是错误码** | 与 A3 区分：模型缺席 → 规则兜底；盒供应商缺席 → 换供应商 / 声明缺货；规则**不**兜语义（规则做不了"转 PDF"）——两条降级路径写进 S3 政策，不混用 |
| 3 · 盒内盲区 | **双列度量 + 消费端验证**：履约率高 + 下游低 = "履约但语义错"——可测量、可降级的状态（供应商降权而非误判健康） | 盒内正确性不在承诺内，接口履约率 + 下游命中是度量面（与模型验证点同构但更强） |
| 4 · EXCLUDED | 出界判定 = **许可（不可再分发）× 接口（无稳定接口）** 双条件；EXCLUDED = **有名有姓的清单**（按动词枚举"为什么不可接"），不是笼统"闭源不行" | 出界完备性由**政策**保证，不由工程保证 |
| 5 · 盒产物注入面 | 产物检疫门（见件④）：返回通道拒绝 + 审计 + 计入履约台账（盒子"履约但产物坏" = 供应商劣化信号） | A1' 扩展 |
| A · 人类直用闭源应用 | 降级为"可用不原生"：帧缓冲流经同一协议对人类窗口开放（远程桌面式）；Agent = 结构化帧协议 | 与 README"loader subset"承诺对齐：**可用性满足，不是原生运行** |

## 3. 生命周期（每供应商五阶段）

1. **注册**：动词-平台-供应商三元组 + cap_exec 授权（域 = 供应商端口）
2. **验证**：黄金轨迹一次录制（参考运行）→ 回放校验 + 与 LEGO 等价物差分
3. **运行**：帧协议（复用 v2 帧头，R3 时延裁决）
4. **度量**：双列台账滚动窗 → 域宽 = f(质量)；**供应商版本变化 = 重新验证**（升级后 golden 重录）
5. **退役**：供应商下线 → 动词转向可选供应商 → 全缺则声明缺货（审计 + 矩阵可见）

## 4. 安全组合（S1×S2×S3 完整适用 + A1'）

| 层 | 对盒子 |
|---|---|
| S1 机制 | 命令通道：动词白名单 + 参数 schema + cap_exec；返回通道：产物检疫门；盒子对内核的唯一权限 = 协议接口，无其他（域天然隔离）→ **定理不变：任何盒输出 ⊆ 域 ∩ 允许产物** |
| S2 测量 | 双列台账（履约率 + 下游成功率），与模型质量同尺度、同入 0x8314 族——**域宽自动适应：盒子变坏 → 域收缩/降权** |
| S3 政策 | 哪些动词允许盒子提供 / 哪些强制 LEGO / 哪些出界——**政策即清单** |

A1–A4 逐字适用 + **A1'（产物）**：任何盒子返回的不可信数据，未经检疫门不得进入任何 Agent 工具链。

## 5. 测量任务（编号修正）

> ⚠ 编号修正：zcode 原方案 B-26/B-27/B-28 与 docs/107 未解之谜表冲突
> （B26 = AHCI 状态机剖析, B27 = ollama 后端取证）→ 顺延为 B-29/30/31。

| 任务 | 内容 | 目的 |
|---|---|---|
| **B-29** | 动词词汇表枚举 | 证明"动词集有限封顶"（几十量级）——接口完备性实证 |
| **B-30** | 盒黄金轨迹回放 | 每供应商一次录制，验证链可复现 |
| **B-31** | 检疫门 fuzz | 畸形产物注入测试（PDF/二进制载荷） |

**论文二主句**（与已定稿方向一致）：*外部智能体家族 = {模型（产文本）, 盒子（产行为）}，同一 S1×S2×S3 信封——盒子不是特例，是家族第二成员；LEGO 是家族的"本地化路径"。*

## 6. 完备性最终声明（canonical wording）

> FujoOS 在"使用"意义下完备：可拆的拆（LEGO·开源语义转录）、可接的接（BOX-BRIDGE·外部能力管道）、不可的出界（EXCLUDED·有名有姓）；**每个动词的能力状态在能力矩阵中可观测，每个供应商的缺席是声明而不是错误，每个盒子的产物在进入系统前经受检疫门**。原生运行（kernel 内装载任意程序）是政策排除项，如实声明。

## 7. 架构图（v0）

```
┌───────────────────── Agent / 人类用户 ─────────────────────┐
│              能力矩阵（动词 × 供应商 × 状态）                  │
└───────────┬──────────────────────────────────┬─────────────┘
      ┌─────▼─────┐                     ┌───────▼───────┐
      │ LEGO 供应商 │                     │ BOX 供应商 n   │
      │ (原生,内核内)│                     │(宿主 Win/VM/  │
      └─────┬─────┘                     │ Wine,内核外)  │
            │                           └───────┬───────┘
      ┌─────▼───────────────────────────────────▼───────┐
      │ Router: V → 供应商链, LEGO > BOX > 缺货声明         │
      ├──────────────────────────────────────────────────┤
      │ 帧协议 v2 (seq/t0/evw/crit/TTL, R3 时延语义)        │
      ├──────────────────────────────────────────────────┤
      │ 动作门(cap_exec·域) │ 产物检疫门(返回通道, S1 扩展)   │
      ├──────────────────────────────────────────────────┤
      │ 双列台账(履约率+下游成功率) → 域宽=f(质量), 与模型同构 │
      └──────────────────────────────────────────────────┘
```

## 8. v0 实现裁剪（本波范围）

| 做 | 不做 |
|---|---|
| 注册表 + 路由（LEGO > BOX > 缺货声明） | GUI 像素流（B-3/v1；人类窗口 = "可用不原生"） |
| 帧协议 v2（模型通道胞弟，首个盒 = **宿主侧供应商**，如宿主 CLI 工具经 shm 通道注册） | 真实 Windows 盒验证（v1 起；v0 只证明"内核外供应商"通路，**不取闭源语义**） |
| 双列台账（履约率先行，下游成功率 = Agent 侧验证点挂接） | 供应商版本自动重验证（v0 手动静默重录即可） |
| 检疫门最小集（魔数白名单 + 大小上限 + 禁执行） | fuzz 全谱（B-31） |
| 缺席声明（per-verb 状态 + 审计） | — |

## 9. 验收（fujoregress 双态用例）

1. **box-online**：盒供应商在线 → 动词调用成功，履约记录入台账 → PASS
2. **box-offline**：盒供应商离线 → 动词声明缺货（审计 + 矩阵可见），系统其余功能不受影响 → PASS
3. **box-badart**：盒返回畸形产物 → 检疫门拒收 + 审计（产物拒收动作码）+ 履约率记败 → PASS

## 10. 与 W34 兼容层关系（不动）

兼容层（shim 翻译）服务**人类直用闭源应用**的"可用不原生"路径；BOX-BRIDGE 服务 **Agent 使用闭源能力**。两者同属 FUFORALL，判定统一走 P1（依赖闭包 ⊆ 提供语义）→ 可翻译 → shim/LEGO；不可翻译但可接口 → BOX；不可接 → EXCLUDED。

## 11. v0.1 修订 — 弱点闭合（DSH 实现者注记）

> 对 v0 骨架（§1–§10）的实现者复核：五条薄弱点逐条闭合。与 zcode 原文
> 冲突处以本节为准；本修订**不推翻五件，只补件（⑥ Adapter）与补语义
> （帧分型/检疫位置/域扩展/可判定性）**。

### 11.1 弱① · Box Adapter 显式组件化（五件 → 六件）

- **定义**：Adapter = 宿主侧程序（脚本/小工具），是**唯一**认识盒世界自动化的组件：
  动词帧 → 盒世界调用（CLI 包装 / COM / 宏 / 文件 watcher"放文件—等产出"）；
  盒世界结果 → 产物 + 状态帧。内核永远只见"供应商 = Adapter + 盒"整体。
- **注册表条目升级**：`(动词 V, 平台 P, 供应商 id, 适配器 id+版本, 状态, 指标)`。
- **信任模型**：Adapter 与盒同属"外层供应商"——内核不装载、不审计其内部；
  Adapter 的契约 = 输入动词帧（已 schema 校验）+ 输出产物帧/状态帧；
  **违约 = 供应商履约失败**（列1 记败），不是独立错误类别。
- **生命周期 §3 修订**：注册阶段增加"Adapter 契约声明（动词 → 调用模板 + 输出 schema）"；
  验证阶段 golden 轨迹 = 录制 **Adapter+盒** 完整调用链（不是只录盒）。
- **姿态声明（与 §8 一致，但 Adapter 被显式承认）**：v0 的盒可以是宿主 CLI 工具（开源），
  证明的是"内核外供应商通路 + Adapter 机制"；闭源语义（winword → PDF）是 **v1 的
  Adapter 内容**，不是 v0 目标。Adapter 是机制件，不是闭源件。

### 11.2 弱② · 产物传输路径 + 检疫门物理位置

- **传输：复用 0xA00000 共享页区 + kind 分域（盒帧 kind = 0xA0+），协议 BOXXFR**：
  产物按 ≤8KiB 块流式发送（seq 连续 + 每块 CRC16 + 全局大小上限；坏块/超时 = 整批丢弃
  并记列1 失败）。
- **组装**：内核侧组装到 tmpfs `/box/{prov}/{verb}/{ts}`（新 VFS 写入口
  `box_xfr_ingest`，唯一入口）。
- **检疫门位置 = 组装写盘前（流式检疫，单一入口）**：魔数白名单（首块）→ 大小上限（累计）
  → 格式校验（末块）→ 禁执行语义（不挂 exec 位）；任一失败 → 整批拒收 + 审计
  （动作码 BOX_GATE_REJECT）+ 列1 记败。
- **v0 带宽注记**：0xA00000 区已钉住 64KiB（模型通道）；盒分域共享，**不增 BSS、
  不动 load_end 硬约束**。若 v1 需高带宽 → 新区方案须先过 load_end 检查（docs/106 坑 #5）。

### 11.3 弱③ · 帧协议分型 TTL（复用裁决器，不复制 hint 时效假设）

R3 语义本质 = **"过期即弃"**；盒帧分三型复用同一裁决器：

| 帧型 | 用途 | TTL 语义 |
|---|---|---|
| CMD（命令帧） | 动词 + 参数 schema | 接受超时：Adapter 须在 t 内 ACK；超时 = STALL → 缺席声明处理 |
| STS（状态帧） | ACCEPTED / RUNNING / DONE / FAILED | 任务执行超时：RUNNING 心跳；超时 = 任务中止 + 列1 记败 |
| XFR（产物块帧） | BOXXFR 块流 | 组装超时：不完整产物 = 拒收（见 11.2） |

- **修正 §3 措辞**："盒子回包同样受 R3 时延裁决" → "盒子回包复用 R3 **过期即弃裁决器**，
  TTL 按帧型配置" —— 产出物型命令**不套** hint 级 TTL（5 分钟前转好的 PDF 依然有效）。

### 11.4 弱④ · 域/台账扩展（审计真实成本）

- **Domain**（capability.rs:299）加字段：`kind`（u64: 0 = 任务/模型域, 1 = 盒供应商域）
  + `prov`（u64 供应商槽）；`DOM_MAX` 5 → 8（v0 用槽 1..=3）。
- **动作集**：`ALL_ACTS` 0x3F → 加 **act 7 = BOX_CMD / act 8 = BOX_XFR**；
  域门对 kind=BOX 域只放行这两动作、全拒其他 —— 域天然隔离保持
  **定理不变：任何盒输出 ⊆ 域 ∩ 允许产物**。
- **台账**：`QUAL_DUTY` 6 → 7（盒履约聚合）/ 8（盒下游可判定聚合）；
  per-provider 指标存注册表；**域宽调整 = dom_admit 扩展 provider 参数**
  （v0 聚合调整，v1 per-provider）。
- **成本估计**：Domain 结构 + 域表宽、dom_create 参数、台账 2 duty、域门逻辑 ≈ 150–250 行
  + 回归用例 2–3 个。

### 11.5 弱⑤ · 列2 验证器可判定性（防软柱子）

- **规则**：`域宽 = f(列1 履约率, 列2a 机器可判定下游谓词)`；
  列2a = 布尔谓词集（文件存在 / 大小 / schema / io 状态码 / 约定产物校验）——
  **禁止 LLM 主观判断进域宽**。
- **列2b（提示性 LLM 观察）** = 展示性信号（能力矩阵"质量详情"行），**不进域宽计算**。
- **与模型 S2 同构**：模型验证点已是机器可判定（plan verify / io hit / conf 阈值）；
  盒子列2a 与其同尺度 —— 论文素材表述："盒子的验证谓词集与模型验证点同尺度"。

### 11.6 修订后验收补充

原三态（box-online / box-offline / box-badart）+ 新增：
4. **box-adapter-fail**：Adapter 违约（输出 schema 不匹配）→ 列1 记败 + 域宽收缩 → PASS。
