# 27 — CI: QEMU 无头 + 日志断言 (M78)

状态: ✅ 完成。本地实测 `fujoci: 25/25 PASS`。

## 1. tools/ci.py

```
fujoci 用例集:
  COMPAT (9):  m30/m33 ELF · m31.run · m32.multi · m29 Mach-O ·
               hello_win/m26/m27/m30 PE32+
  MILESTONES (16): m61..m69 + m71..m77 的 "MXX RESULT: PASS" 断言

流程: taskkill qemu → QEMU 256M (串口 file: log + 模型链 tcp)
      → sleep 7.5s (boot) → monitor telnet sendkey "os run hermes"
      → 轮询 (默认 20s 上限) → 断言 needle in log → 报告/退出码
参数: --only IX (单用例) / --json out.json / --timeout S
```

## 2. GitHub Actions (.github/workflows/ci.yml)

```
windows-latest:
  choco llvm qemu → cargo build --release (kernel)
  → flatten (--pad 0x1A0000) → python tools/ci.py --json ci-results.json
  → upload-artifact (报告)
```

## 3. 实测

```
:: [24] m77-win          PASS
fujoci: 25/25 PASS
```

25/25: 兼容 9/9 + 里程碑 16/16 (M61-M77 全部日志断言)。

## 4. 增量

- 新里程碑 demo 加入 MILESTONES 一行即纳入 CI;
- 真盘/网卡/多核用例以同 run_one 机制扩展 (参数化 initrd/needle)。
